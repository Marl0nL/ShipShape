# ShipShape egress allow-list — managed by the control plane (`shipshape`).
#
# One domain per line; a leading dot matches subdomains (.github.com).
# Lines beginning '#' are comments (ignored by Squid). The control plane uses a
# sentinel to represent a DISABLED (unchecked) domain WITHOUT deleting it:
#     #SS-OFF# .github.com     <- disabled; flip back to `.github.com` to enable.
# Any other '# ...' line is treated as a section header and left untouched, so
# your hand-written headers below survive round-trips.
#
# Apply changes live (no restart, no dropped connections):
#     docker exec egress-proxy squid -k reconfigure
# The `shipshape` control plane runs this for you on every change.

# --- GitHub ---
.github.com
.githubusercontent.com

# --- firstmate toolchain (treehouse / no-mistakes installers + self-update) ---
kunchenguid.github.io

# --- Google Cloud / gcloud ---  (.googleapis.com covers oauth2/storage/etc.)
.googleapis.com
accounts.google.com
packages.cloud.google.com
dl.google.com
.pkg.dev
.gcr.io

# --- Claude Code / Anthropic ---
api.anthropic.com
claude.ai
platform.claude.com
downloads.claude.ai

# --- Antigravity CLI (agy) — VERIFIED working through Squid on agy 1.1.2 ---
# (agy honors HTTPS_PROXY and tunnels via CONNECT; auth is copy/paste OAuth — `agy-login`.)
antigravity.google
antigravity-unleash.goog
antigravity-cli-auto-updater-974169037036.us-central1.run.app
.googleusercontent.com
# (accounts.google.com, oauth2/cloudaicompanion/cloudcode-pa/play/generativelanguage
#  .googleapis.com, and storage.googleapis.com are already covered above.)

# --- npm registry (runtime `npm i -g` by agents) ---
registry.npmjs.org

# --- Python / PyPI ---
pypi.org
files.pythonhosted.org

# --- Dart / Flutter ---  (storage.googleapis.com covered by .googleapis.com above)
pub.dev
.gstatic.com

# --- Approved via control plane ---
.repositpower.com
