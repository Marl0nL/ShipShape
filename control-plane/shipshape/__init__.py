"""ShipShape control plane.

Host-side management for the containerised agent sandbox: egress allow-list
control, denied-domain harvesting, and (later phases) credential injection and
an approval queue for agent-requested domains, commands, and auth refreshes.
"""

__version__ = "0.1.0"
